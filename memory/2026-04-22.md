# 2026-04-22

- Added durable auto-router rules to `~/.hermes/SOUL.md`.
- Corrected CLI availability detection: `codex` and `gemini` are available in interactive bash shells via `/data/pnpm` (`/data/pnpm/codex`, `/data/pnpm/gemini`), while Hermes' default non-interactive PATH may omit `/data/pnpm`.
- Router now checks both default and interactive shell contexts for CLI availability, prefers Gemini first for discovery when external-agent routing is useful, and uses Claude as fallback.
